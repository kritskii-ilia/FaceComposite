pywebview>=5.0
vosk>=0.3.45
sounddevice>=0.4.6

# --- Нейросетевое понимание описания (этап 1, nlu.py). Всё локально/офлайн. ---
# numpy обязателен (на нём работает бесзависимостный hashing-бэкенд).
numpy>=1.24
# Опционально: семантическая модель эмбеддингов для лучшего понимания синонимов.
# Без неё nlu.py сам падает на hashing-бэкенд. Модель скачивается ОДИН раз и
# дальше работает офлайн; для автономного .exe положите её в models/nlu и
# укажите путь (см. app.py NLU_MODEL_DIR). Рекомендуемая модель:
#   sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
# sentence-transformers>=2.2.0
